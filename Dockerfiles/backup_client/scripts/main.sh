#!/bin/bash

# Clear files BEFORE and AFTER
rm -rf "${HOLDERFOLDER}/*"

echo "hi"


#echo "$PASSWORD" | sftp -P ${PORT} ${USER}@${HOST}:${REMOTE_DIR} <<< 'put ${local_file_path}'




rm -rf "${HOLDERFOLDER}/*"


tail -f /dev/null
